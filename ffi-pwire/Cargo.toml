[package]
name = "pyrosql-ffi-pwire"
version = "1.1.0"
edition = "2021"
description = "C-ABI shared library for PWire (raw TCP SQL client)"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
lz4_flex = "0.11"
